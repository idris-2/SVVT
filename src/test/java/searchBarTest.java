import org.junit.jupiter.api.Test;

public class searchBarTest extends BaseClass{
    @Test
    public void searchFilterTest(){
        webDriver.get(baseUrl);
        // Impossible because of the CloudFlare blocker
    }
}
